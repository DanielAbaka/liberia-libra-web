/**
 * Organizations Liberia Libra has delivered work for—paid engagements or volunteer
 * projects. Use `placeholder: true` for open slots; replace with real entries when ready.
 * Optional `url` turns the card into a link.
 *
 * @typedef {{ id: string, placeholder?: true } | { id: string, name: string, role: string, url?: string }} PartnerRow
 * @type {PartnerRow[]}
 */
export const PARTNERS = [
  {
    id: "orange-liberia",
    name: "Orange Liberia",
    role: "Live media coverage—events, campaigns, and on-site activations.",
  },
  {
    id: "orange-money-liberia",
    name: "Orange Money Liberia",
    role: "Live media coverage—product storytelling, field promos, and activations.",
  },
  {
    id: "edtel-events",
    name: "Edtel Events",
    role: "Live media coverage—events, campaigns, and on-site activations.",
  },
];
